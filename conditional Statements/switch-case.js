var user = "User";

switch(user){
    case "Super Admin":
        console.log("You have full access to the system!");
        break;
        case "Admin":
        console.log("You have full access to the system, but can't delete anything!");
        break;
        case "User":
        console.log("You have full access to the system, but can only view the recordS!");
        break;
}